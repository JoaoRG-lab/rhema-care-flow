# DESATIVADO — substituído pelo TMR Deployment Auditor
# Workflow ativo: tmr-deploy.yml

name: Build, Lint & Security Audit (DISABLED)

on:
  workflow_dispatch:
    inputs:
      reason:
        description: 'Este workflow está desativado. Use tmr-deploy.yml'
        required: false

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Este workflow está desativado. O TMR Auditor cuida do CI.'
